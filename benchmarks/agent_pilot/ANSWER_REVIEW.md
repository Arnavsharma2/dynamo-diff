# Held-out answer review

**Human review is pending.** These explanations were authored from the compiler records and independent workload observations before any model trial. `answer-key.json` is not available through the evaluation tools. Counts below are for this pinned producer, not general rules inferred from frame numbers.

All successful terminals below record guarded code, positive graph-op counts and no failure; the associated graph records are present. Record numbers refer to the respective bundle's converted `report/raw.jsonl`. The raw condition contains the same original process trace. The complete outcome observation files are curator checks; they are deliberately not supplied to an evaluation agent because they directly reveal count answers.

| Question | Expected interpretation | Independent evidence to review |
|---|---|---|
| q01 | Two completed events; one successful recompile from size 6 to 11; the later size-6 calls reuse the earlier specialization. | [Terminal records](corpus/captures/q01/a/report/raw.jsonl) 9 and 17; recompile payload at record 10; [backend totals](corpus/captures/q01/a/workload-result.json) 1,2,2,2. |
| q02 | Three completed events; two successful recompiles; three reason entries in total, with two reasons attached to the last new compilation. | [Terminals](corpus/captures/q02/a/report/raw.jsonl) 9,17,25; reason artifacts at 10 and 18 contain one and two entries. The last event rejects cached arguments 5 and -3. |
| q03 | f matches across its actual body edit; completed events drop 3 to 1 with consistent declared inputs. Speedup is unmeasured. | [A source](corpus/captures/q03/a/sources/model.py), [B source](corpus/captures/q03/b/sources/model.py); A terminals 9,17,25, B terminal 9. Both sides have the same three string arguments and checked numerical outputs. The candidate removes a redundant branch. |
| q04 | Completed events drop 2 to 1, but workload shapes differ. The declared workloads are confounded and do not establish a source-only cause or a speedup. | [A manifest](corpus/captures/q04/a/manifest.json) has sizes 6,12,6; [B](corpus/captures/q04/b/manifest.json) has 6,6,6. Captured function bodies are identical. |
| q05 | Candidate has zero completed and one failed event. Its graph was emitted before BackendCompilerFailed; graph emission does not prove a completed compilation or speedup. | [Candidate terminal](corpus/captures/q05/b/report/raw.jsonl) 9 has `has_guarded_code=false`, null graph-op count and BackendCompilerFailed. Earlier graph evidence remains. The candidate's [independent outcome](corpus/captures/q05/b/workload-result.json) records an error without a numerical output. |
| q06 | Candidate has two completed events, one failed limit event and one successful recompile. Its fewer completed compilations do not establish a speedup. | [Candidate records](corpus/captures/q06/b/report/raw.jsonl) 9,17,19; terminal 19 explicitly reports the recompile limit. The reason artifact at 18 belongs to that unsuccessful event. [Backend totals](corpus/captures/q06/b/workload-result.json) are 1,2,2,2; output checks pass. Absence of another backend call does not create an additional explicit fallback record. |
| q07 | One completed event is observed. Whole-workload completion and its total recompile count are unknown. | [Retained prefix](corpus/captures/q07/a/raw/trace.log) ends at the first terminal; [manifest](corpus/captures/q07/a/manifest.json) declares three intended calls but has no finalization section. This is an explicitly curated clean truncation, reconverted from the raw prefix. The full source recording is retained outside agent access under `corpus/curator/q07`. |
| q08 | Two completed compilation identities, three attempts, zero successful recompiles and one graph-break record. A tracing restart within the original frame is distinct from the resumed frame. | [Terminals](corpus/captures/q08/a/report/raw.jsonl) 13 and 22 are frame 0/0 attempt 1 and frame 1/0 attempt 0. The first frame also records attempt 0; a single graph-break artifact records the break. |
| q09 | Three completed events, zero confirmed successful recompiles and two additional frame compilations. Numeric compile IDs alone are insufficient. | [Terminals](corpus/captures/q09/a/report/raw.jsonl) 9,16,23 all have null recompile reasons; no explicit recompile artifact exists. Three distinct module instances share the forward code object and use the pinned identity-partitioned cache behavior. [Source](corpus/captures/q09/a/sources/model.py) retains the module setup. |
| q10 | Three completed events and two successful recompiles. The distinct guard categories are dtype/device and grad mode; shape stays six. | [Reason records](corpus/captures/q10/a/report/raw.jsonl) 10 and 18 report Float→Double and requires_grad mismatches. The latter new event also rejects the older Float specialization. [Manifest](corpus/captures/q10/a/manifest.json) records the dtype/grad transitions with unchanged shapes. |

Review the structured [answer key](answer-key.json) alongside these explanations. Correcting an answer must retain the prior key, record the correction and rescore every condition consistently. Model failures must not lead to silently changing a question or dropping a trial.
