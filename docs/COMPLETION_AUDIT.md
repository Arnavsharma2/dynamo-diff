# Scope completion audit

Completed September 7, 2026. The [approved scope](PROJECT_SCOPE.md) is the acceptance contract, including the user's revision making independent human review optional. The [original proposal](PROJECT_SCOPE_ORIGINAL.md) is preserved. **All required portfolio-v1 deliverables and acceptance gates are satisfied for the recorded environments.** This is an AI audit; no human review, external adoption or public package release is claimed.

| Requirement | Current evidence | Remaining work or limit |
|---|---|---|
| Pin and verify an adapter/toolchain | [Compatibility](COMPATIBILITY.md), hashed dependency locks, retained PyTorch 2.14.0 / tlparse 0.4.3 captures | Only the recorded combination is verified; the research-reference tlparse commit is not advertised as tested. |
| Inspect saved captures without ML runtimes | The source-map validation revision passes 96 installed-wheel tests and both offline demonstrations without ML runtimes, locally and in hosted macOS/Linux checks | Verified Python 3.13 environments are listed in the compatibility guide; other Python versions and Windows remain unverified. |
| Recover identities and nested attempts | Twelve authored scenarios, including graph-break restart and separate module identity groups; [AI review guide](../fixtures/REVIEW.md) links expectations to original records and backend observations | Independent human review is optional and unclaimed. |
| Separate success, failure, no graph and unknowns | Positive guarded-code/graph/terminal checks, backend failure, no-graph and malformed/truncated tests | Missing evidence stays unknown; graph emission alone is not success. |
| Explicit recompile reasons and reason association | Scalar/shape/grad/identity captures; multiple reasons attached to one event; increasing IDs without recompile evidence remain additional compilations. CLI text/Markdown retain guard categories from both sides, including removal in the edit pair | Supported text categories are documented; no universal guard taxonomy claim. |
| Preserve graph breaks, limits and fallback evidence | Graph-break and compiler-limit fixtures; failure/fallback comparisons retain `not_measured` performance conclusions | No fallback record is invented where the producer did not emit one. |
| Conservative matching across ordinary edits | Authored edit pair passes automatic body-edit matching despite changed hashes and frame IDs; unmatched helper retained | Ambiguous/renamed/split/generated matches remain unresolved when unsupported. |
| Preserve baseline and candidate source | Immutable snapshots, captured-line source navigation and no substitution of live source | Exact causal expression is not claimed from a function-level attribution. |
| Separate capture validity and workload completion | Missing/clean-truncated records, strict finalization fields and manifest hash checks | Cleanly ending JSON is not proof of a completed workload. |
| Display workload comparability | Ordered calls, compiler/backend settings, source-independent producer/workload changes, missing fields and explicit automatic dynamic policy covered by tests | `manifest_consistent` describes declarations; it is not runtime-equivalence proof. |
| Capture recipe and manifest template | [Capturing](CAPTURING.md) and retained generator manifests | Analyzer never launches a workload to fill missing fields. |
| Versioned CLI/JSON/Markdown interfaces | CLI/core parity, checked-in capture/comparison schemas, structured errors, terminal and Markdown output | Public schema 1, adapter revision 4 and comparison rules 4 are distinct version contracts. |
| Immutable evidence and bounded storage | Content-addressed import, artifact hashes, path/symlink checks, reserved-path protection, aggregate limits, strict JSON and side-effect regression | Guard/source text remains untrusted data; import does not execute it. |
| Cache identity and invalidation | Input, producer/manifest, adapter/schema and analysis-option hashes tested; comparison identity adds matching options and rules | Comparisons are recomputed rather than persistently cached. |
| Input boundary audit | [AI code/evidence audit](INPUT_AUDIT.md) inspected parser, store, CLI/MCP and editor boundaries. Conflicting/non-object source maps were reproduced and corrected; 18 focused comparison tests pass | Audit assumptions and limits are explicit; independent human review is not claimed. |
| Shared MCP tools with small responses | Real stdio test calls all three tools; index discovery exposes evidence omitted from compact rows; pagination and root restrictions tested | Agent-response correctness is measured separately in the pilot. |
| Local processing and agent privacy boundary | No telemetry or model dependency in analyzer; [integration guide](INTEGRATIONS.md) explains possible model-provider transmission | Local MCP does not promise local inference for arbitrary clients. |
| VS Code commands, results and navigation | Ten development-host checks and eleven installed-VSIX checks passed in VS Code 1.132.1 on macOS and Linux, including parity, cancellation, read-only source/evidence, error recovery, active-group reuse and rendered-table activation. Native baseline/candidate source, rendered table and original guard evidence are visually verified in one editor group | Candidate `compute` opens at captured line 9, with `VARIANT = "after"` and the scalar branch removed. Inspection covers the observed macOS profile and window size. |
| Measured processing costs | Retained 10 MiB / 100 MiB import trials, RSS, warm comparison latency, output size and separate conversion costs in [Performance](PERFORMANCE.md) | The validation revision increased import time about 42%; both versions remain recorded and unchanged regression thresholds pass. These are tool-processing measurements. |
| Real-project diagnosis with correctness evidence | Full unmodified Transformers 5.10.1 `generate()` case, before/after traces and separate eager-model token/logit oracle in the [AI-reviewed case study](../case_studies/transformers_cache/README.md) | Human review is optional and unclaimed. CPU recording backend supports no CUDA/Inductor or application-speed claim. A second case is an aspiration, not an essential gate. |
| Held-out three-condition diagnostic pilot | All 60 planned attempts finished; all individual results and failures are retained. The strict primary result demonstrates no end-to-end improvement. Frozen evaluated code is archived separately from the subsequent MCP navigation change; AI answer/evidence review is documented | Mechanical scores remain provisional; optional human answer-key and semantic review have not occurred. Frozen records retain their historical protocol. The exploratory answer-member analysis is explicitly post-hoc and does not replace the primary result. |
| Installable package and VSIX | Current wheel passes 96 checks and both offline demonstrations without ML runtimes; 12 evaluation-harness tests pass. The unchanged VSIX passed eleven isolated-install checks and was installed in the approved profile | [Delivery receipt](../artifacts/portfolio-v1-delivery.json) records final package hashes and payload checks. Nothing has been published to package registries. |
| CI, licenses and documentation | [Hosted CI passed](../artifacts/HOSTED_CI.md) for the uploaded private repository: macOS/Linux installed-wheel jobs and Linux development/installed-VSIX checks. Pinned action commits, MIT project license, exact upstream licenses/attribution and architecture, semantics, compatibility and release guides are retained | Recorded environments only; no public package release or human review is implied. |
| Final demonstration and review | The [short CLI recording](DEMO.md) retains five successful commands, original output and a hash receipt; offline demonstrations reproduce. Native visual inspection is complete, and AI review materials link direct evidence | All required delivery checks are complete. Independent human review, external demand validation and public distribution are optional follow-ups. |

The [verification record](../artifacts/VERIFICATION.md) and [portfolio-v1 delivery receipt](../artifacts/portfolio-v1-delivery.json) support the final claims. The final audit inspected the approved requirements, relevant test assertions and receipts, capture/reproduction guides, shared MCP/editor implementation, evaluation records, package contents and native rendered views. Earlier source-map, review-policy, build and wheel/profile receipts remain historical: their hashes and open-gate notes describe those checkpoints and must not be treated as current delivery status.

Demand validation is a later decision target in the original scope: engineers using their own traces and actionable feedback remain unclaimed. No outreach has been sent. Passing local tests or authoring this audit does not establish external use, human approval, a published release or improved application performance.
