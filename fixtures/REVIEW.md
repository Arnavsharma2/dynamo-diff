# Controlled capture review

**AI-reviewed; independent human review is optional.** These explanations were prepared and inspected by Codex against the retained compiler records and the separately instrumented backend. Human review has not occurred. The user made it optional in the September 7 [scope revision](../docs/PROJECT_SCOPE.md). Numeric expectations remain in [expected.json](expected.json).

The links below refer to the original tlparse metadata and payloads, not the analyzer's normalized output. Line numbers are one-based records in `report/raw.jsonl`. Each bundle also retains the original prefixed process log under `raw/`, its manifest, the executed source snapshot and `workload-result.json`. The latter records backend invocation totals and output comparisons independently of Dynamo Diff. Backend invocations alone are not a general definition of completed Dynamo compilation.

For success, inspect the terminal record together with its graph and guarded-code evidence. Count one compilation identity per rank/frame/frame-compile tuple, with attempts nested beneath it. Count guard-reason entries separately from new compilations. None of these diagnostic captures measures application speed.

| Case | Expected interpretation | Direct records and independent check |
|---|---|---|
| Stable repeated inputs | One completed compilation, one attempt, no recompilation. | [Metadata](captures/stable/report/raw.jsonl), terminal 9. [Backend totals](captures/stable/workload-result.json): 1, 1, 1. |
| Static shape A → B → A | Two completed compilations and one successful recompilation; the final A reuses an existing variant. | [Metadata](captures/shape/report/raw.jsonl), terminals 9 and 17, reason record 10. [Reason](captures/shape/report/-_0_1_0/recompile_reasons_4.json) says size 4 → 8. Backend totals 1, 2, 2. |
| Python scalar | Two completed compilations, one successful recompilation and one scalar guard reason. | [Metadata](captures/scalar/report/raw.jsonl), terminals 9 and 17, reason record 10. [Reason](captures/scalar/report/-_0_1_0/recompile_reasons_4.json) concerns `scale == 1`; inputs use scale 1, 2, 1. Backend totals 1, 2, 2. |
| Multiple guards | Three completed compilations and two successful recompilations, with three reason entries in total. The last new compilation has two rejected cached specializations. | [Metadata](captures/multiple_guards/report/raw.jsonl), terminals 9, 17 and 25, reason records 10 and 18. Inspect both linked reason payloads; their lengths are one and two. Backend totals 1, 2, 3. |
| Dynamic generalization | Two completed compilations and one successful recompilation. The generalized second graph serves the third shape. | [Metadata](captures/dynamic/report/raw.jsonl), terminals 9 and 18, reason record 10. [Backend graphs and totals](captures/dynamic/workload-result.json) include a `SymInt` input in the second graph; shapes 4, 8, 16 produce totals 1, 2, 2. This does not prove generalization to all shapes. |
| Grad requirement | Two completed compilations and one successful recompilation caused by a recorded `requires_grad` mismatch, while shape stays fixed. | [Metadata](captures/grad/report/raw.jsonl), terminals 9 and 17, reason record 10. [Reason](captures/grad/report/-_0_1_0/recompile_reasons_4.json). Backend totals 1, 2, 2. |
| Graph break and restart | Two completed compilation identities, three internal attempts, one break record/site and no confirmed recompilation. The first frame restarts at attempt 1; a resumed frame compiles separately. | [Metadata](captures/graph_break/report/raw.jsonl), break record 7, terminals 13 (`frame=0`, `attempt=1`) and 22 (`frame=1`, `attempt=0`). [Backend totals](captures/graph_break/workload-result.json): 2, 2. Do not count the abandoned attempt as a third completed compilation. |
| Backend failure | One failed compilation and zero completed compilations, even though a graph was emitted and the backend was invoked once. | [Metadata](captures/backend_failure/report/raw.jsonl), error record 8 and terminal 9 (`has_guarded_code=false`, `BackendCompilerFailed`). [Workload result](captures/backend_failure/workload-result.json) records the intentionally caught exception. Harness completion is separate from compilation success. |
| No graph | One observed identity with a no-graph outcome; zero completed graph compilations and zero backend calls. | [Metadata](captures/no_graph/report/raw.jsonl), terminal 7 has no graph operation count; no graph artifact was emitted. [Source](captures/no_graph/sources/compiler_cases.py) returns its input unchanged. [Backend totals](captures/no_graph/workload-result.json): 0, 0. Frame processing alone does not establish an installed compiled graph. |
| Compiler limit | Three observed identities: two completed and one failed at the limit. Two identities have explicit recompilation evidence, but only one recompilation completed successfully. | [Metadata](captures/limit/report/raw.jsonl), terminals 9, 17 and 19; the last is `Unsupported` with the compiler-limit reason. Reason records 10 and 18. [Backend totals](captures/limit/workload-result.json): 1, 2, 2, 2. Later correct outputs do not turn the limit into a speedup; no explicit fallback record is fabricated. |
| Module identity | Three completed compilations, two successful recompilations and three guard-reason entries across distinct module instances sharing `forward`. | [Metadata](captures/identity/report/raw.jsonl), terminals 9, 17 and 25, reason records 10 and 18. Guard payloads contain `___check_obj_id(self, ...)`; backend totals 1, 2, 3. |
| Separate identity groups | Three completed compilations, zero confirmed recompilations and two additional frame compilations. | [Metadata](captures/identity_partitioned/report/raw.jsonl), terminals 9, 16 and 23 have increasing frame-compile IDs but no explicit recompile reasons. [Source](captures/identity_partitioned/sources/compiler_cases.py) sets `torchdynamo_force_dynamic=False` per instance. Backend totals 1, 2, 3. Numeric IDs alone cannot establish recompilation. |

The [authored edit demonstration](../tools/demo.py) separately checks an ordinary body edit, shifted frame IDs and an unmatched helper while preserving both [before](captures/edit_before/sources/model.py) and [after](captures/edit_after/sources/model.py) snapshots. Malformed/truncated inputs, path escapes, duplicate imports and ambiguous matches are controlled mutations in the test suite; they are not represented as newly recorded upstream behavior.

The [Transformers investigation](../case_studies/transformers_cache/README.md) supplies the real-project explanation and its independent eager-output oracle. The held-out [pilot answer review](../benchmarks/agent_pilot/ANSWER_REVIEW.md) is a separate key. Reviewing this file does not imply reviewing that key or the agent responses.

## Recording a review

A future human reviewer should identify corrections by case and record, or explicitly confirm inspection of the explanations and supporting evidence. Record the reviewer, date, material inspected and changes. Keep the AI-reviewed label until a human review actually occurs; the optional review does not block portfolio-v1 delivery.
